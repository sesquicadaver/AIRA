# Model unavailable

## What is this?

A required local model is not ready or not selected.

## What to do?

Use built-in calculation when possible, or prepare a model via the model layer.

## What should happen?

Deterministic tasks can still succeed without an LLM.

## If it failed?

Do not expect VERIFIED text generation without an activated backend.
