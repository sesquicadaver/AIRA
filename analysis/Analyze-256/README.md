# Analyze-256 — ProblemRecord split (QUEUE #221)

## Done
- `ProblemRecord.execution_artifact_id` persisted for generate-local
- Executed rows no longer store the exec id in `verified_artifact_id`
- GET remaps legacy executed-as-verified index rows
- RFC-0115; RFC-0111 still file-free

## Out
`network=none` contract RFC (`#222`); RFC-0111; Landlock; Core inference.
