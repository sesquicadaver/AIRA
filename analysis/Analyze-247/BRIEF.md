# BRIEF — Analyze-247

Bind Reduction catalog so non-math / non-echo / non-upper problems select `text.generate.local` (RFC-0105 payload) after `#211` DONE. `Calculate 2 + 2` stays `math.eval.safe`. Bind by action string; do not Cargo-dep execution-llm. Do not register the plane (`#213`), implement activate (`#214`), or file RFC-0104.
