# Analyze-228 — Runtime Clock (QUEUE #193)

## Done
- `Clock` / `SystemClock` / `FixedClock` in `aira-object`
- CSU/plane emits use `aira_object::now()`
- `local_session_artifacts_are_not_all_mvp_fixed_timestamp`; RFC-0091
- QUEUE `#193` **DONE**; first OPEN `#194`

## Out
Envelope freshness/replay (`#194`); run nonce (`#195`); instance-scoped clock (`#196`).
