# Analyze-97 — quarantine fetch local source (QUEUE #62)

## Status
OPEN (branch `analyze-97-quarantine-fetch`).

## Done when
After ALLOW: `--source` → `<root>/models/quarantine/…` + Event; no activation.

## Out
Hash verify; activate; remote URL; sharing; C1/core.
