# Analyze-193 — Sqlite object path beside memory (QUEUE #158)

## Status
DONE — docs + coexistence test + Core↛node manifest check.

## Done when
`open_node_sqlite_object_store`; `plane_memory_beside_node_sqlite_object_path`; local-node / operational-plane docs; `aira_core` has no node/peer deps.

## Out
Stores status rollup (#159); plane drain switching to SQLite.
