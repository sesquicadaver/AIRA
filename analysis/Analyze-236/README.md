# Analyze-236 — Seal `object_store_access` (QUEUE #201)

## Done
- Feature `store-backend` on `aira-object`; enabled only by `aira-core`
- Default prelude has no `object_store_access`; C0 opacity uses `ObjectStore::create`
- RFC-0097; Opaque Handle **DONE**
- QUEUE `#201` **DONE**; first OPEN `#202`

## Out
VRA runtime B1-010 (`#202`); event-log authority; anti-mission.
