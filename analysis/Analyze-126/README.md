# Analyze-126 — Windows login autostart (QUEUE #91)

## Status
OPEN — Analyze-126 / PR pending.

## Done when
Startup `.bat` write/remove for `autostart_on_login`; Linux/macOS preserved; tests.

## Out
zip package; codesign; Registry Run.
