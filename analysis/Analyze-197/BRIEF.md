# Analyze-197 — QUEUE #162 federation export/import deny

## Goal
Policy-scoped federation export/import: deny-by-default, durable `io-policy.json`, trust-audit on DENY.

## Out of scope
C3 cases ≥6 (#163).
