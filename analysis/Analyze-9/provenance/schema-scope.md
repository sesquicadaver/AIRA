# Provenance — Schema registry scope

## Delivered
- 20 canonical schemas from Schema Pack (C0/C1 + conformance report + context/epistemic)
- Alias copies `policy-query` / `policy-decision`
- Fixtures manifest: 17 valid + 5 invalid
- `aira-schema` inlines `aira:schema:…` `$ref` before jsonschema validate

## Not delivered (intentional)
- Protocol envelope/response, identity, settlement, research schemas (C2+/C4+/C5)
- Differentiated field / human choice (optional MVP stub later)

## Originals
`Manifesto etc/` and `Meditation_About/` unchanged; schemas extracted as copies into `schemas/`.
