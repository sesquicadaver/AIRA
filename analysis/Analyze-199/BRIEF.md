# Analyze-199 — QUEUE #164 optional C3 CI

## Goal
Add non-required `conformance-c3` job to ci.yml; governance restates not a merge gate.

## Out
CRP schema fixtures (#165); promoting C3 to required checks.
