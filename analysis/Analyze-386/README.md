# Analyze-386 — Work executor + capability readiness

Work Auto/specific preference; math ready without model; generate needs tip/required available; pre-submit gate; RFC-0232; tip → first OPEN `#350`.
