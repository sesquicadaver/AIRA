# Analyze-90 — LocalModelInventory payload schema (QUEUE #55)

## Status
OPEN (Phase D D0.3).

## Done when
JSON schema `aira:schema:model:inventory:0.1` + valid/invalid fixtures; `schema validate --fixtures` green. RFC-S recorded. Canonical `ArtifactType` unchanged.

## Out
CompatibilityEvidence / Policy schemas; `aira models scan|list`; Inventory CSU runtime; network; downloader; `ArtifactType::LocalModelInventoryArtifact`; Core/C1/plane changes.
