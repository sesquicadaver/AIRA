# Rules — Analyze-42

- CSU emits must use tenant-scoped helpers (`make_*_as` with `csu_id`)
- Tenant signing secrets must not be inserted into process Keyring signing map
- Plane / primary_signer path untouched
- No Manifesto / Meditation / peer / gossip
