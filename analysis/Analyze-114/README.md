# Analyze-114 — Linux packaging (QUEUE #79)

## Status
CLOSED @ 04995ab / PR #42 (QUEUE #79 DONE).

## Done when
install without `cargo`; `#78` autostart paths valid (`Exec=aira-desktop` on PATH).

## Out
macOS DMG; Windows installer; AppImage; P1.
