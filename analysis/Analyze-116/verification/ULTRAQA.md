# UltraQA — Analyze-116

1. P1 write without peer_listen → default 127.0.0.1:9797.
2. P2 write fails.
3. Schema still accepts P2 enum (runtime Out); no invalid-P2 schema fixture.
4. Peer process not started (Out #82).

Verdict: **PASS**.
