# Analyze-180 — Linux deb metapackage (QUEUE #150)

## Status
DONE @ PR #113 — deb stage script + package smoke.

## Done when
`scripts/package-desktop-linux-deb.sh --layout-only` package smoke; doc in `desktop-packaging.md`.

## Out
AppImage.
