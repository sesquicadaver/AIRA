# Provenance — Plane Node Signing

Deferred from Analyze-21: OperationalPlane signs emits with node identity.

Mechanism: process primary signer + redirect of `local_identity` / `local_signature*` helpers used by plane and basic CSUs.
