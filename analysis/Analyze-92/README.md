# Analyze-92 — ModelAcquisitionPolicy schema (QUEUE #57)

## Status
OPEN (Phase D D0.5).

## Done when
JSON schema `aira:schema:model:acquisition-policy:0.1` with required `auto_download` (valid fixture `false`) + valid/invalid fixtures; `schema validate --fixtures` green. RFC-S recorded. Canonical `ArtifactType` unchanged.

## Out
Downloader; allowlist runtime; Inventory CLI; compatibility resolver; policy DENY runtime (`#60`); Core/C1/plane changes.
