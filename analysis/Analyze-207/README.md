# Analyze-207 — settlement fixtures (QUEUE #172)

## Done
- Schema `aira:schema:settlement:receipt:0.1` with required `privacy_class`
- Valid fixture + invalid unsigned / raw_prompt (PRIV-001)
- QUEUE `#172` DONE → `#173` OPEN

## Out
Receipt store (`#173`); B2-011 smoke (`#174`).
