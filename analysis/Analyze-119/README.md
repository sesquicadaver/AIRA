# Analyze-119 — invite QR PNG (QUEUE #84)

## Status
CLOSED @ da5c800 / PR #47 (QUEUE #84 DONE).

## Done when
PeerInvite ↔ PNG encode/decode (file); roundtrip tests; CLI `invite-export-qr` / `invite-import-qr`.

## Out
Camera / live scan; GUI (`#85`).
