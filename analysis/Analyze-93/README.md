# Analyze-93 — local inventory read-only (QUEUE #58)

## Status
OPEN (Phase D D1).

## Done when
`aira models scan|list` works; Inventory CSU with scoped FS; scan without network/download; immutable inventory artifact published.

## Out
Compatibility resolver (`#59`); download / policy DENY runtime (`#60`); C1/plane/Book 0 changes; canonical `ArtifactType`.
