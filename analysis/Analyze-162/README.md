# Analyze-162 — Split crypto mod (QUEUE #127)

## Status
OPEN — branch `queue-127-split-crypto-mod`.

## Done when
Mechanical split: crypto integration tests in `smoke_tests.rs`; `mod.rs` re-exports only; `cargo test -p aira-object` green.

## Out
Crypto behavior change; SEC hardening (#134).
