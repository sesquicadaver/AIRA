# Analyze-361 — Admission snapshot type + persist

**QUEUE:** `#324` · **RFC-0209**

## Outcome

`AdmissionSnapshot` frozen at ProblemSubmitted; OperationalArtifact + Context factor + ProblemRecord persistence. Text-only defaults; richer API deferred to `#325`.
