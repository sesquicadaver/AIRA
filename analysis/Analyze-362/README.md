# Analyze-362 — Submit API carries constraints

**QUEUE:** `#325` · **RFC-0210**

## Outcome

HTTP/CLI/Desktop carry optional `AdmissionConstraints` frozen at admit via `submit_problem_with_admission`. Mid-run Settings do not mutate the admitted snapshot.
