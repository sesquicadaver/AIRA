# Analyze-369 — Math capsule fidelity

**QUEUE:** `#332` · **RFC-0216**

## Outcome

Removed default `2+2` on math capsules. Expressions for `9-3` / `9/3` / `42` (and Calculate forms) are preserved through reduction→execution→verification; unsupported math fails closed without a substituted VRA.
