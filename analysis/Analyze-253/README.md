# Analyze-253 — Activate evidence (QUEUE #218)

## Done
- `ActivatedPointerGate` in `crates/aira-flow/src/activate_gate.rs`: required Phase D pointer fields, cache under `models/`, hash of cache bytes, Evidence artifact + `activated=true` + cryptographic signature
- `install_fixture` for tests/HTTP
- Forged `{model_ref}` → CapsuleFailed
- RFC-0112; RFC-0111 still file-free

## Out
Child env whitelist (`#219`); bounded pipes; ProblemRecord split; network RFC; RFC-0111; Landlock; Core inference.
