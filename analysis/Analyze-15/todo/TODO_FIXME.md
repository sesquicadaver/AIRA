# TODO / FIXME — Analyze-15

- [ ] YAML `config.yaml` parity with bootstrap plan (JSON is MVP)
- [ ] Persist Object Store writes on submit into SQLite (init creates DB only)
- [ ] Wire registered CSU handlers from `.aira/csu/registry.json` into Runtime (plane uses built-in basic set)
- [ ] Sign artifacts/events with local Ed25519 identity (still TESTSIG in flow helpers)
- [ ] SQLite event log (JSON event-log is MVP)
