# Analyze-77 — Object canonical signatures (QUEUE #42)

## Status
OPEN (implementation on branch; close after merge).

## Done when
Object sign/verify uses canonical descriptor JSON without `signature`. Mutation of object fields fails verify. Object store create admits only canonical signatures.

## Out
Event/Artifact (already #40–#41); CSU manifests; envelope.
