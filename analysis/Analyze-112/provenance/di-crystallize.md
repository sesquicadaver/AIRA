# Provenance — Analyze-112

1. Launcher is XDG menu only; no tray (`#78`).
2. Autostart-on-login remains `#78` (separate from applications `.desktop`).
3. Template embedded via `include_str!` so install works without repo checkout.
