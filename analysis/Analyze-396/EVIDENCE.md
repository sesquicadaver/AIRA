# Evidence — P4 process gate

See [`run.json`](run.json) and [`BRIEF.md`](BRIEF.md).

This file exists so the gate has a human-readable record next to the JSON. It does not add claims beyond that JSON.

C1 default CI remains `c1.pipeline.process_executor_executed` (Executed ≠ VERIFIED) plus VRA schema fixtures. That is not this run and not a runtime VRA pipeline.
