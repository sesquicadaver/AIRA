# Analyze-188 — C3 governance note (QUEUE #153)

## Status
DONE — docs + contract test; no CI job.

## Done when
`docs/ci-governance.md` + `docs/conformance.md` state C3 is optional (not merge gate), document criteria for optional job (`#164`) and for future required-gate promotion; `ci_governance_doc` asserts no `conformance-c3` in `ci.yml` yet.

## Out
`conformance-c3` workflow job (`#164`); C3 case expansion; CRP; making C3 a required check.
