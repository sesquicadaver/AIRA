# Provenance — Analyze-113

1. Primary UI = egui window (Status/Settings/Quit); not Tauri/web.
2. StatusNotifier tray icon deferred; autostart + window satisfy #78 Done when.
3. Autostart Exec=`aira-desktop` (not `aira desktop start`).
