# Analyze-115 — PeerInvite schema (QUEUE #80)

## Status
OPEN — `aira:schema:desktop:peer-invite:0.1`.

## Done when
schema + fixtures; `schema validate --fixtures` green.

## Out
settings P1; peer process; QR; GUI.
