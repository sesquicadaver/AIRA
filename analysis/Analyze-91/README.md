# Analyze-91 — ModelCompatibilityEvidence schema (QUEUE #56)

## Status
OPEN (Phase D D0.4).

## Done when
JSON schema `aira:schema:model:compatibility-evidence:0.1` with required `reason` / `confidence` / `scope` + valid/invalid fixtures; `schema validate --fixtures` green. RFC-S recorded. Canonical `ArtifactType` unchanged.

## Out
AcquisitionPolicy schema; compatibility resolver runtime; auto-download; rating score; Core/C1/plane changes.
