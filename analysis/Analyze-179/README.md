# Analyze-179 — Windows MSI (QUEUE #149)

## Status
DONE @ PR #112 — WiX stage + layout-only CI artifact.

## Done when
`scripts/package-desktop-windows-msi.sh --layout-only` builds CI artifact; doc section in `desktop-packaging-windows.md`.

## Out
Authenticode / enterprise codesign.
