# Analyze-251 — Desktop + RFC-0104 (QUEUE #216)

## Done
- Work tab / `POST /v1/problems` documents and covers generate-local (same node path as `aira problem submit`)
- Human-first UX: lead with `result.result` + `executed` status; identifiers and JSON stay Details
- Generate without Phase D activate remains fail-closed (HTTP error; never fake VERIFIED)
- C1 `Calculate 2 + 2` unchanged (`math.eval.safe` / execution-basic)
- Consolidating RFC [`AIRA-RFC-0104`](../../specs/rfc/AIRA-RFC-0104-phase-k-local-llm-csu.md)
- QUEUE `#209`–`#216` **DONE**; QUEUE K closed; no OPEN K atoms
- CI still MockBackend (no ollama)

## Out
llama/ggml in aira-core; CI process backend; GPU marketplace; LLM-in-Core; blockchain; a new QUEUE phase after K.
