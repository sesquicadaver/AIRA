# BRIEF — Analyze-251

Close Phase K: Desktop Work generate path through existing `POST /v1/problems` (Reduction #212 + plane #213 + MockBackend default). Human-first UX: `result.result` + status, not raw VRA JSON. C1 `Calculate 2 + 2` stays math.eval.safe. Create consolidating RFC-0104. QUEUE K closed. Do not invent Phase L. Do not add llama/ggml to aira-core. Do not change CI to require process backend.
