# BRIEF — Analyze-246

Add `csu/execution-llm` with MockBackend after `#210` DONE. Valid `text.generate.local` capsules complete via mock; missing backend or invalid payload fail closed (`CapsuleFailed`, not fake VERIFIED). Do not bind Reduction (`#212`), register the plane (`#213`), implement activate (`#214`), or file RFC-0104.
