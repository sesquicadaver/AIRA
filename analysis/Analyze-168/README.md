# Analyze-168 — Invite QR camera (QUEUE #133)

## Status
OPEN — GUI `Scan QR (camera)` + `import_invite_qr_luma` runtime path.

## Done when
Roundtrip smoke: encode → luma decode → trust+book; GUI camera button; tests green.

## Out
Non-loopback peer dial; invite schema change.
