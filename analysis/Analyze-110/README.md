# Analyze-110 — Desktop settings schema (QUEUE #75)

## Status
CLOSED (QUEUE #75 DONE — PR pending merge).

## Done when
`aira:schema:desktop:settings:0.1` + fixtures; `schema validate --fixtures` green.

## Out
orchestrator; GUI; token generation; P1+.
