# Analyze-122 — macOS LaunchAgent (QUEUE #87)

## Status
CLOSED @ b1498d1 / PR #50 (QUEUE #87 DONE).

## Done when
plist write/remove for `autostart_on_login`; Linux XDG preserved; tests.

## Out
`.app` bundle; notarize; launchctl bootstrap.
