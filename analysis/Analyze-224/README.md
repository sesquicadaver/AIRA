# Analyze-224 — Durable reuse index (QUEUE #189)

## Done
- `problems/reuse-index.json`: problem-text SHA-256 → verified artifact id
- `LocalSession::submit_problem` seeds Reduction from the index; missing CAS id falls through
- `local_session_repeat_problem_reuses_without_execution`; RFC-0087
- QUEUE `#189` **DONE**; first OPEN `#190`

## Out
Fail-closed signing (`#190`); atomic persist (`#191`); knowledge-catalog reuse.
