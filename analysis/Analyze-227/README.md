# Analyze-227 — Artifact metadata recovery (QUEUE #192)

## Done
- Per-`artifact_id` files under `descriptors/`
- Shared CAS blob is not a second-descriptor wipe
- `second_descriptor_same_content_hash_recoverable`; RFC-0090
- QUEUE `#192` **DONE**; first OPEN `#193`

## Out
Clock (`#193`); envelope replay (`#194`); dropping the legacy `{hash}.json` sidecar.
