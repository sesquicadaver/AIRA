# Analyze-163 — Split aira-peer lib (QUEUE #128)

## Status
CLOSED @ 87ed2bd / PR #91 (QUEUE #128 DONE).

## Done when
Mechanical split: peer integration tests in `smoke_tests.rs`; `lib.rs` re-exports only; `cargo test -p aira-peer` green.

## Out
Protocol/peer behavior change.
