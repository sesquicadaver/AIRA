# BRIEF — Analyze-225

Fail-closed signing: `active_signature` has no silent local-test fallback; demo/test primary is explicit; LocalSession does not swallow identity errors. RFC-0088. Do not start atomic persist (`#191`).
