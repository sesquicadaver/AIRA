# Analyze-195 — capability advertisement persist (QUEUE #160)

## Done
- `aira-protocol::CapabilityAdvertisementStore` load/save + register validation
- Tests: `capability_ad_persist_roundtrip`, reject node/unsigned
- Docs: local-node layout; implementation-status; QUEUE `#160` DONE → `#161` OPEN

## Out
C3 capability advertisement case (#161).
