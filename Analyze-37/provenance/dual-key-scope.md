# Provenance — dual-key node grace

Keyring multi-verify per key_ref; persist previous pubkey + grace_until on identity JSON.
Out: peer notify (A-38); CRL-based same-id grace; X25519 coordinated rotate.
