# Analyze-69 — Public HTTP bind opt-in (QUEUE #34)

## Scope
`--allow-public-bind` for non-loopback `--listen` / `--health-listen`. Default fail-closed. Loopback unchanged. TLS/Bearer stay independent.

## Done when
Test refusal without flag; docs path; no federation.
