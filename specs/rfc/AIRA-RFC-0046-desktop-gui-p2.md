# AIRA-RFC-0046 — Desktop GUI P2 profile

## 1. Summary

GUI profile selector P0|P1|P2 (`#96`); P2 peer status labels include `dht+apply-book`. Invite UX unchanged.

## 5. Non-Goals

P3+ Advanced; camera; lifecycle flags (`#95` done).

## 15. Tests

`cargo test -p aira-desktop actions::tests::p2_profile_persist_peer_listen`
