# AIRA-RFC-0022 — local upgrade recommendation publish (RFC-R)

`publish_recommendation` in `aira:csu:model.recommendation` writes CustomArtifact + `models/recommendation.latest.json` + Event. Evidence required. network=none. Out: CLI `#74`, marketplace.
