# Analyze-70 — Federation join prototype (QUEUE #35)

## Scope
Local self-signed descriptor → TrustStore pin + `federation/membership.json`. CLI `aira federation join --descriptor`. Not a network handshake. Other members stay Untrusted.

## Done when
Join+trust durable; Living Spec; no CRP/settlement.
