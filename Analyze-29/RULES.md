# Правила Analyze-29

Per-CSU publisher identity.

## Hard rules
1. Do not edit `Manifesto etc/**` or `Meditation_About/**`
2. Emit signing must fail closed if publisher has no signing key
3. Do not change plane primary signing for ProblemStatement/seed events
4. Document in `docs/crypto.md`
